/**
 * Type that defines what properties the parsed JSON objects will have for Pz fans Project Zomboid maps
 */
type PzFanMapData = {
    id: string;
    label: string;
    description: string;
    url: string;
}
